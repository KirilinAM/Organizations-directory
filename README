# Запуск
Через `docker-compose up` с переменными среды:
- DB_HOST --- Хост БД 
- DB_PORT --- Порт БД
- DB_NAME --- Имя БД
- DB_USER --- Имя пользователя БД
- DB_PASSWORD --- Пароль БД
- API_KEY --- Ключ доступа к АПИ
- SQL_ECHO --- Флаг вывода SQL-скриптов в консоль

# API
- Список API в интерактиной документации: http://localhost:8000/docs.
- Для использования большинства эндпоинтов требуется авторизация апи-ключом через X-API-Key в заголовке. Апи ключ указывали в API_KEY.